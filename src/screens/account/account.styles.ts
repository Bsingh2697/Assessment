import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{ justifyContent:'center',alignItems:'center',flex:1, },
    switchView:{ height: 50, width: 50, justifyContent: 'center', alignItems: "flex-end" },
    accountText:{ fontSize:20,marginBottom:8 }
})

export {styles}