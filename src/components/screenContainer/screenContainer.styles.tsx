import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    justifyContent: 'space-around',
  },
  headerView: {flexDirection:'row', paddingHorizontal:16, alignItems:'center',paddingBottom:14}
});

export { styles };
